import { RouterConfig } from '@angular/router';

import { QueryComponent } from './index';

export const QueryRoutes: RouterConfig = [
  {
    path: 'system/query',
    component: QueryComponent
  }
];
